{
  "name": "bhopal_real_estate",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd client && npm install && npm run build",
    "start": "node server.js",
    "build": "cd client && npm install && ./node_modules/.bin/vite build",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "mongoose": "^9.6.2",
    "nodemailer": "^8.0.7",
    "sqlite3": "^6.0.1",
    "twilio": "^6.0.2"
  }
}
